# Staging status

The staging-copy inventory and original hashes remain in staging-manifest.json. Source and notebook portability changes are documented in reproducibility.md. The source hashes in the historical frozen method intentionally remain original; preprocessing_provenance.json records the unchanged transformer implementation across migration. Completed reports, split/fold manifests, settings, raw files, fitted artifact and saved predictions are unchanged. The employer-facing root README and four PNG/SVG figures are complete. No Git repository or publication has been created.
